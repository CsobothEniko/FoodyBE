create table ch (
	id SERIAL PRIMARY KEY,
	name varchar(255),
	quantity integer,
	quantity_type varchar(255),
	calorie integer,
	type varchar(255)
);

create table fat (
	id SERIAL PRIMARY KEY,
	name varchar(255),
	quantity integer,
	quantity_type varchar(255),
	calorie integer,
	type varchar(255)
);

create table fruit (
	id SERIAL PRIMARY KEY,
	name varchar(255),
	quantity integer,
	quantity_type varchar(255),
	calorie integer,
	type varchar(255)
);

create table protein (
	id SERIAL PRIMARY KEY,
	name varchar(255),
	quantity integer,
	quantity_type varchar(255),
	calorie integer,
	type varchar(255)
);

create table vegetable (
	id SERIAL PRIMARY KEY,
	name varchar(255),
	quantity integer,
	quantity_type varchar(255),
	calorie integer,
	type varchar(255)
);

create table vitamin (
	id SERIAL PRIMARY KEY,
	name varchar(255),
	quantity integer,
	type varchar(255)
);

create table plan (
	id SERIAL PRIMARY KEY,
	ch_id integer,
	fat_id integer,
	pr_id integer,
	fr_id integer,
	veg_id integer,
	type varchar(255)
);

create table diet_plan (
	id SERIAL PRIMARY KEY,
	breakfast_id integer,
	lunch_id integer,
	dinner_id integer,
	weekday integer
);


create table user_data (
	id SERIAL PRIMARY KEY,
	last_name varchar(255),
	first_name varchar(255),
	username varchar(255),
	password varchar(255)
	
);

create table user_table(
	id SERIAL PRIMARY KEY,
	email varchar(255),
	username varchar(255),
	password varchar(255)

);

-- ez csak összekötő tábla, nem kell létrehozni
create table user_role(
	

);

create table role(
	id SERIAL PRIMARY KEY,
	name varchar(255),
	

);


create table breakfast (
	id SERIAL PRIMARY KEY,
	ch_id integer,
	fat_id integer,
	pr_id integer,
	fr_id integer,
	veg_id integer,
	vitamin_id1 integer,
	vitaminid2 integer
	
);

create table lunch (
	id SERIAL PRIMARY KEY,
	ch_id integer,
	fat_id integer,
	pr_id integer,
	fr_id integer,
	veg_id integer,
	vitamin_id1 integer,
	vitaminid2 integer
	
);

create table dinner (
	id SERIAL PRIMARY KEY,
	ch_id integer,
	fat_id integer,
	pr_id integer,
	fr_id integer,
	veg_id integer,
	vitamin_id1 integer,
		
);





